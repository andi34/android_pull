#!/bin/bash
echo ""
echo ""
cd device/samsung/espresso-common
echo "----------------------------------------"
echo "move some more parts to espresso-common"
git fetch https://gerrit.omnirom.org/android_device_samsung_espresso-common refs/changes/82/16082/3 && git cherry-pick FETCH_HEAD
echo "----------------------------------------"
cd ../../..
echo "----------------------------------------"
echo "----------------------------------------"
cd device/samsung/p3100
echo "----------------------------------------"
echo "move some more parts to espresso-common"
git fetch https://gerrit.omnirom.org/android_device_samsung_p3100 refs/changes/83/16083/1 && git cherry-pick FETCH_HEAD
echo "----------------------------------------"
cd ../../..
echo "----------------------------------------"
echo "----------------------------------------"
cd device/samsung/p3110
echo "----------------------------------------"
echo "move some more parts to espresso-common"
git fetch https://gerrit.omnirom.org/android_device_samsung_p3110 refs/changes/85/16085/1 && git cherry-pick FETCH_HEAD
echo "----------------------------------------"
cd ../../..
echo "----------------------------------------"
echo "----------------------------------------"
cd device/samsung/p5100
echo "----------------------------------------"
echo "move some more parts to espresso-common"
git fetch https://gerrit.omnirom.org/android_device_samsung_p5100 refs/changes/84/16084/1 && git cherry-pick FETCH_HEAD
echo "----------------------------------------"
cd ../../..
echo "----------------------------------------"
echo "----------------------------------------"
cd device/samsung/p5110
echo "----------------------------------------"
echo "move some more parts to espresso-common"
git fetch https://gerrit.omnirom.org/android_device_samsung_p5110 refs/changes/86/16086/1 && git cherry-pick FETCH_HEAD
echo "----------------------------------------"
cd ../../..
echo "----------------------------------------"
echo "----------------------------------------"
cd kernel/samsung/espresso10
# echo "Nothing for now"
# echo "enforcing time"
# git fetch https://gerrit.omnirom.org/android_kernel_samsung_espresso10 refs/changes/91/12091/1 && git cherry-pick FETCH_HEAD
echo "Overclocking stuff"
git fetch https://gerrit.omnirom.org/android_kernel_samsung_espresso10 refs/changes/35/14335/2 && git cherry-pick FETCH_HEAD
git fetch https://gerrit.omnirom.org/android_kernel_samsung_espresso10 refs/changes/36/14336/2 && git cherry-pick FETCH_HEAD
git fetch https://gerrit.omnirom.org/android_kernel_samsung_espresso10 refs/changes/37/14337/2 && git cherry-pick FETCH_HEAD
git fetch https://gerrit.omnirom.org/android_kernel_samsung_espresso10 refs/changes/38/14338/2 && git cherry-pick FETCH_HEAD
git fetch https://gerrit.omnirom.org/android_kernel_samsung_espresso10 refs/changes/39/14339/2 && git cherry-pick FETCH_HEAD
git fetch https://gerrit.omnirom.org/android_kernel_samsung_espresso10 refs/changes/40/14340/2 && git cherry-pick FETCH_HEAD
echo "----------------------------------------"
cd ../../..
echo "----------------------------------------"
echo "----------------------------------------"
cd hardware/samsung
echo "nothing for now..."
echo "----------------------------------------"
cd ../..
echo "----------------------------------------"
echo "----------------------------------------"
cd hardware/ti/omap4
echo "nothing for now..."
echo "----------------------------------------"
cd ../../..
echo "----------------------------------------"
echo "----------------------------------------"
cd frameworks/native
echo "nothing for now..."
cd ../..
echo "----------------------------------------"
echo "----------------------------------------"
cd frameworks/base
echo "nothing for now..."
cd ../..
echo "----------------------------------------"
echo "----------------------------------------"
cd frameworks/av
echo "nothing for now..."
cd ../..
echo "----------------------------------------"
echo "----------------------------------------"
cd frameworks/opt/telephony
echo "nothing for now..."
cd ../../..
echo "----------------------------------------"
echo "----------------------------------------"
cd vendor/omni
echo "vendor: add date and time by default for HOMEMADE builds  https://gerrit.omnirom.org/#/c/11379/"
git fetch https://gerrit.omnirom.org/android_vendor_omni refs/changes/79/11379/6 && git cherry-pick FETCH_HEAD
cd ../..
echo "----------------------------------------"
echo "----------------------------------------"
cd build
"add back make bootzip"
git fetch https://gerrit.omnirom.org/android_build refs/changes/63/14663/1 && git cherry-pick FETCH_HEAD
git fetch https://gerrit.omnirom.org/android_build refs/changes/64/14664/1 && git cherry-pick FETCH_HEAD
cd ..
echo "----------------------------------------"
echo "----------------------------------------"
echo ""
echo "Done!"
echo "----------------------------------------"
echo "----------------------------------------"
